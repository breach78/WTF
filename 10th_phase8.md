# 길동 10고 Phase 8 - 중첩 트리 재구성

- 대상 패키지: `/Users/three/Documents/시나리오 작업/ver3/1st_app.wtf`
- 시나리오 ID: `B6F21CB6-BC46-42FA-B500-6AA87BB2D468`
- `ai` 루트 ID: `C0DAD9E7-4E76-4F03-9E3E-177D47B195B0`
- 재구성 스크립트: `/Users/three/app_build/wa/phase8_rebuild_nested_tree.mjs`
- 검산 스크립트: `/Users/three/app_build/wa/phase8_verify_nested_tree.mjs`
- 카드 ID 맵: `/Users/three/app_build/wa/10th_phase8_card_ids.json`
- 검산 결과 JSON: `/Users/three/app_build/wa/10th_phase8_verification.json`

## 1. 목표

기존 `4막 -> 개요 / 시놉시스 / 시퀀스 / 트리트먼트` 형제 분기 구조를 폐기하고, 사용자가 요청한 아래의 실제 부모-자식 구조로 다시 짰다.

```text
시작 / 중간1 / 중간2 / 끝
  -> 개요
    -> 시놉시스
      -> 시퀀스
        -> 트리트먼트
          -> 원문
```

## 2. 실제 변경

- 기존 visible 분기 카드 `개요 / 시놉시스 / 시퀀스 / 트리트먼트` `16개`를 모두 `archived` 처리
- 기존 visible 개요 카드 `12개`를 모두 `archived` 처리
- 기존 visible 시놉시스 카드 `8개`를 모두 `archived` 처리
- 새 중첩 구조용 개요 카드 `10개` 신규 생성
- 새 중첩 구조용 시놉시스 카드 `11개` 신규 생성
- 기존 시퀀스 `11개`는 그대로 살리고, 새 시놉시스의 자식으로 재부모화
- 기존 트리트먼트 `21개`는 그대로 살리고, 각 시퀀스의 자식으로 재부모화
- 기존 원문 카드 `116개`는 그대로 살리고, 각 트리트먼트의 자식으로 유지

즉 이번 단계에서 바뀐 핵심은 `상위 구조의 배치`이며, 시퀀스 이하 카드 자산과 원문 카드는 최대한 유지했다.

## 3. 새 구조의 수량

- 4막: `4`
- 개요: `10`
- 시놉시스: `11`
- 시퀀스: `11`
- 트리트먼트: `21`
- 원문 씬: `116`
- visible descendant 총계: `173`

중첩 포인트:

- `개요 04 | 041-060` 아래에 `시놉시스 04 | 041-053` + `시놉시스 05 | 054-060`을 배치해, 개요가 실제로 더 자세한 하위 시놉시스로 분기되도록 재구성했다.

## 4. 원문 무결성

재부모화 이후 전수 검산에서 원문 카드 `021`, `022` 두 장의 끝 공백이 손실된 이력을 발견했다.

조치:

- 기준 원고 슬라이스로 `021`, `022` 원문 카드만 다시 덮어씀
- 이후 `116개` 원문 카드 전수를 다시 읽어 동일성 검산 완료

최종 상태:

- 원문 불일치: `0`
- 해시 불일치: `0`
- 바이트 수 불일치: `0`

## 5. 앱 반영 확인

- 기존 `WTF`를 종료 후 재실행 완료
- 재실행 후에도 새 중첩 구조가 그대로 로드됨
- 확인 표본:
  - `중간1 -> 개요 04 -> 시놉시스 04 / 05 -> 시퀀스 04 / 05 -> 트리트먼트 -> 원문`

## 6. 결론

- `4막 -> 개요 -> 시놉시스 -> 시퀀스 -> 트리트먼트 -> 원문` 실중첩 구조로 재구성 완료
- 기존 분기형 상위 구조는 `archived`
- 원문 `116개` 전수 동일성 검산 통과
- 앱 재실행 및 구조 로드 확인 완료
